(test
  (name kernel1Par)
  (modules kernel1Par)
  (libraries types generate kernel1Par qcheck unix domainslib)
)

(test
  (name kernel2Par)
  (modules kernel2Par)
  (libraries types generate kernel1Par kernel2Par unix domainslib qcheck)
)
